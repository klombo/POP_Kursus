# bind a name l to a list composed of the first 20 integers starting at 1
# l = list(range(1,20))
# print(l)

# bind a name ll to a list composed of all integers between 12 and 42
# ll = list(range(12,42))
# print(ll)

# what is the length of the list ll?
# print(len(ll))

# what is the first element of the list ll?
# print(ll[0])

# create a list lll obtained by removing the first element from the list ll
# lll = ll[0]
# print(lll)

# define a function that takes one argument and returns a list composed of this element
# def xList(x):
#     return [] + []

# x = xList(10)
# print(x)

# define a function that  takes a list of strings as parameter and returns a boolean indicating whether the list is empty or not. 
# Define this function without using list properties.
# def listIsEmpty(listeAfOrd):
#     return listeAfOrd == []
# print(listIsEmpty([]))

# define a function that  takes a list of strings as parameter and returns a boolean indicating whether the list is empty or not. 
# Define this function without using if then else.
# def listIsEmpty(listAfOrd):
#     return len(listAfOrd) == 0

