(* let mutable x = 0
while x < 10 do
    printfn "%d" (x)
    x <- x + 1 *)

(* let mutable x = 1
while x % 5 <> 0 do
    printfn "%d" x
    x <- x + 1 *)

(* let mutable z = 0
while z < 15 do
    printfn "#"
    z <- z + 1 *)

(* let mutable k = 17
while k <= 1000 do 
    printfn ("%d") k
    if k = 1 then k <- k * 2 else
    k <- k * k *)

(* let mutable swapValue1 = 42
let mutable swapValue2 = 24
let mutable t = 0

printfn "temp Value: %A" (t)
printfn "swap Value 1: %A" (swapValue1)
printfn "swap Value 2: %A" (swapValue2)

t <- swapValue1
swapValue1 <- swapValue2
swapValue2 <- t

printfn "temp Value: %A" (t)
printfn "swap Value 1: %A" (swapValue1)
printfn "swap Value 2: %A" (swapValue2) *)


